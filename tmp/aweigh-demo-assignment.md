# App behavior and demonstration assignment

Model: Opus. Launch with `--permission-mode auto`. Use `/private/tmp/aweigh-showcase-team/` as the report workspace, with the three source repositories supplied through `--add-dir`.

Read `aweigh-complexity-brief.md` beside this file. Own current app behavior tracing and demonstration design. Source repositories are read-only during this first assignment. Write `/private/tmp/aweigh-showcase-team/demo-direction.md`. Other agents are investigating archive evidence concurrently. Preserve their files and all existing changes. Do not spawn subagents.

Read the current Aweigh scenario server, screenshot capture guides, watch-grid guide, and schedule design principles. Inspect current Swift source and worker code enough to separate implemented behavior from historical proposals. Review the current portfolio and Aweigh detail page; both contain uncommitted user work that must be retained.

Propose three short demonstrations that show engineering judgment and a rider consequence. Identify each code path, fixture format, and exact way to reproduce the state with existing tooling. List any narrowly scoped capture-tool changes required. If no app changes are necessary, say so. The archive investigator will supply real event evidence separately; do not invent timestamps or statistics.

The first-screen site currently emphasizes hiring availability, and the Aweigh page already has a watch video, a static screen-size/state matrix, and an island-selection map. Improve the demonstration rather than duplicating those elements. A visitor should understand a consequence in roughly ten seconds, with an optional deeper explanation. Recommend a small, feasible implementation that uses native app output without rebuilding the app in the browser. Do not commit to the scenario-selector idea if a better format emerges.

Explain visual structure, interaction, and copy using concrete content. Distinguish archived examples from live data, observed behavior from predictions, and actual app behavior from a web illustration. No em dashes in user-facing text. Do not publish, deploy, send messages, expose contact details, or access credentials. If Xcode inspection is needed, use Xcode MCP; use CLI for builds. This assignment does not require building or changing SwiftUI.

Return the proposed sequence, source references, capture feasibility, and a precise scoped implementation brief. Stop after writing the report; implementation follows evidence review.
